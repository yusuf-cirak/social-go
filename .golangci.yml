# @format

# golangci-lint configuration
run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gofmt
    - goimports
    - misspell
    - gocritic
    - gosec
    - bodyclose
    - exportloopref
    - gocognit
    - goconst
    - gocyclo
    - godot
    - mnd
    - goprintffuncname
    - nestif
    - prealloc
    - rowserrcheck
    - sqlclosecheck
    - unconvert
    - unparam
    - whitespace

linters-settings:
  gocognit:
    min-complexity: 15
  gocyclo:
    min-complexity: 15
  mnd:
    checks:
      - argument
      - case
      - condition
      - operation
      - return
      - assign
  gosec:
    excludes:
      - G401
      - G501
  misspell:
    locale: US

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - mnd
        - goconst
    - path: "cmd/migrate/"
      linters:
        - mnd
    - path: "internal/db/seed/"
      linters:
        - mnd
        - gosec
  max-issues-per-linter: 0
  max-same-issues: 0
